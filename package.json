{
  "name": "shadcnui-foundry",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-framework component generation pipeline inspired by shadcn/ui",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "test": "turbo run test",
    "test:visual": "playwright test",
    "test:visual:update": "playwright test --update-snapshots",
    "handoff:all": "node ./scripts/handoff-all-frameworks.mjs",
    "parity:scorecard": "node ./scripts/parity-scorecard.mjs",
    "typecheck": "turbo run typecheck",
    "clean:foundry": "pnpm --filter @shadcnui-foundry/cli build && pnpm --filter @shadcnui-foundry/cli run foundry -- clean",
    "clean": "turbo run clean && pnpm clean:foundry && node -e \"require('node:fs').rmSync('node_modules', { recursive: true, force: true })\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=./packages/* && changeset publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "@biomejs/biome": "^1.9.4",
    "@changesets/cli": "^2.27.12",
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3",
    "rimraf": "^6.0.1",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.9"
}
